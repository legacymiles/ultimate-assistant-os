import { SmartShot } from "@/components/smart-shot/SmartShot";

export const metadata = {
  title: "Smart Shot Videos · Ultimate Assistant OS",
  description:
    "One prompt and your photos become an editable storyboard sheet — character reference, set design, floor plan, cuts, lighting — then MiniMax H3 renders every cut.",
};

export default function Page() {
  return <SmartShot />;
}
