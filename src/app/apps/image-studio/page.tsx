import { ImageStudio } from "@/components/image-studio/ImageStudio";

export const metadata = {
  title: "Image Studio · Ultimate Assistant OS",
  description:
    "Specialist image agents — Nostalgia, Glamour, Family Portrait, Cartoon → Real and GTA Life. Pick one, write a prompt, add reference photos and get styled images back.",
};

export default function Page() {
  return <ImageStudio />;
}
